---
tags: [frontend, карта]
---

# Карта экранов

← [[Edvantix]] · [[Dashboard (школа)]] · [[Admin (операторская)]] · задачи: [[Бэклог]]

Инвентарь экранов обоих приложений: что существует и в каком состоянии.
Открытые доработки — задачи `EDX-NNN` в [[Бэклог|Бэклоге]], порядок — [[Этапы внедрения]].

## Легенда

✅ готово · 🔧 есть открытая задача (`EDX-NNN`) · 🔴 удалено · 🟡 создать

## clients/dashboard

| Экран                      | Маршрут                      | Статус | Что сделать                                                           |
| -------------------------- | ---------------------------- | ------ | --------------------------------------------------------------------- |
| Обзор                      | `/`                          | ✅      | индексный редирект по роли (этап 6) + полоса школьных показателей для менеджера (этап 7): активные ученики/группы, занятия за месяц, долг по школе из `totalCount` существующих списков; каждая плитка гейтится правом своего эндпоинта |
| **Кабинет**                | `/my`                        | ✅      | стартовая страница кабинета: плитки + «ближайшие занятия» и (для ученика/представителя) сводка по счетам; для преподавателя — «мои группы». Роль — по `GET /people/me/scope` + правам. Плашка «доступ к материалам ограничен из-за задолженности» (EDX-015) по `GET /student-invoices/my/materials-access` — также на `/my/invoices` и в карточке занятия |
| **Приём приглашения**      | `/accept-invite`             | ✅      | публичный маршрут (вне `ProtectedRoute`): форма установки пароля по ссылке из письма (`email`/`token`/`tenant` из query) → тот же `POST /identity/reset-password`, что и `/reset-password` → редирект на `/login` |
| ~~Каталог: товары~~        | ~~`/catalog/products`~~      | 🔴     | убрано (этап 7): экраны, `api/catalog.ts`, маршруты, вкладки корзины и команды палитры удалены; `PagedResponse` вынесен в `api/pagination.ts` |
| ~~Каталог: категории~~     | ~~`/catalog/categories`~~    | 🔴     | убрано (этап 7)                                                       |
| ~~Каталог: бренды~~        | ~~`/catalog/brands`~~        | 🔴     | убрано (этап 7)                                                       |
| ~~Карточка товара~~        | ~~`/catalog/products/:id`~~  | 🔴     | убрано (этап 7)                                                       |
| Чат                        | `/chat`                      | ✅      | на карточке группы — ссылка «Чат группы» по `chatChannelId` (этап 7); в `channel-rail` каналы учебных групп вынесены в секцию «Study Groups» (по `ChannelDto.sourceStudyGroupId`, EDX-010) со ссылкой-стрелкой на `/study-groups/:id` ([[EDX-011 Раздел каналов учебных групп в чате]]) |
| Обращения                  | `/tickets`                   | ✅      | категория (фильтр, метка в списке, выбор при создании) + привязка к ученику/счёту (комбобоксы в диалоге, ссылки на карточке) — готово (этап 7) |
| Файлы                      | `/files`                     | ✅      |                                                                       |
| Пользователи               | `/identity/users`            | ✅      |                                                                       |
| Роли                       | `/identity/roles`            | ✅      |                                                                       |
| Группы доступа             | `/identity/groups`           | ✅      | переименовано в UI в «Группы доступа» (меню + командная палитра); маршрут/эндпоинты не тронуты (этап 7) |
| Подписка                   | `/subscription`              | ✅      | [[Billing]]; отдельный раздел меню «Подписка» (этап 7)               |
| Счета подписки             | `/invoices`                  | ✅      | в разделе «Подписка», отдельно от «Счетов учеников» (этап 7)         |
| Аудит                      | `/audits`                    | ✅      | в карточке события — читаемые названия сущностей и полей через `GET /api/v1/audits/entity-labels` (этап 7) |
| Активность                 | `/activity`                  | ✅      | в разделе меню «Система» (этап 7)                                     |
| Сессии                     | `/system/sessions`           | ✅      |                                                                       |
| Корзина                    | `/system/trash`              | ✅      | вкладки Courses / Tickets / Files; вкладки Catalog убраны, `trash-permissions.ts` обновлён (этап 7) |
| Настройки                  | `/settings/*`                | 🔧 [[EDX-009 Экран API-ключей в настройках]] | личные вкладки (профиль/безопасность/оформление/уведомления/API-ключи), RU-подписи; настройки школы вынесены в раздел «Школа» → `/school/*`. Вкладка «API-ключи» — заглушка «coming soon» (нет бэкенда — [[EDX-008 API-ключи в Identity]]), плюс ссылка на чужой репозиторий fullstackhero |
| Здоровье                   | `/health`                    | ✅      |                                                                       |
| **Ученики**                | `/students`                  | ✅      | список, фильтры, статусы, импорт CSV — готово                          |
| **Карточка ученика**       | `/students/:id`              | 🔧 [[EDX-006 Убрать устаревшую заглушку на карточке ученика]] | профиль/представители/заметки/группы/посещаемость/счета-баланс/**история** готовы; вкладка «История» — аудит по сущности (`GET /api/v1/audits/by-entity/Student/{id}`, гейт `Permissions.AuditTrails.View`, метки полей через `entity-labels`, строки раскрываются до изменённых полей), EDX-005; на вкладке «Обзор» осталась мёртвая заглушка «Дальнейшие разделы» |
| **Преподаватели**          | `/teachers`                  | ✅      | список — готово                                                       |
| **Карточка преподавателя** | `/teachers/:id`              | ✅      | профиль/учётка; секция «Нагрузка» (активные группы/занятия/часы, окно 7/14/30 дней, `GET /teachers/{id}/workload`, гейт `Scheduling.Sessions.View`); секция «Группы преподавателя» (`GET /study-groups?teacherId=`) со ссылками на карточки групп; ссылка «Расписание преподавателя» → `/schedule?teacherId=` (EDX-004) |
| **Представители**          | `/guardians`                 | ✅      | список + карточка с блоком «подопечные» (`GET /guardians/{id}/students`) — готово |
| **Направления**            | `/subjects`                  | ✅      | дерево, инлайн CRUD, перестановка — готово                             |
| **Курсы**                  | `/courses`                   | ✅      | список, фильтры, статусы, сортировка, корзина — готово                 |
| **Конструктор курса**      | `/courses/:id`               | ✅      | карточка, жизненный цикл, разделы/уроки, автосохранение, материалы — готово |
| **Учебные группы**         | `/study-groups`              | ✅      | список, фильтры (курс/преподаватель/статус/формат), сортировка, создание — готово |
| **Мои группы**             | `/my/groups`                 | ✅      | список групп (преподаватель/ученик/подопечные) без создания/редактирования; `/study-groups/my` → редирект на `/my/groups` |
| **Карточка группы**        | `/study-groups/:id`          | ✅      | конструктор + отчёт посещаемости + ссылка на шаблоны расписания + ссылка «Чат группы»; в диалоге зачисления выбор тарифа, тариф виден в ростере; действие «Сменить тариф» в ростере (`PUT /enrollments/{id}/tariff`, гейт `Enrollments.Update`, EDX-007) |
| **Расписание**             | `/schedule`                  | ✅      | календарь неделя/месяц (FullCalendar), фильтры группа/преподаватель/аудитория (фильтр преподавателя дип-линкуется: `?teacherId=`), drag-перенос с разрешением 409-конфликта, SignalR — готово |
| **Карточка занятия**       | `/sessions/:id`              | ✅      | тема, материалы урока, посещаемость, провести/перенести/отменить, read-only после терминального статуса — готово. При задолженности (EDX-015) блок «Материалы урока» показывает плашку вместо списка |
| **Моё расписание**         | `/my/schedule`               | ✅      | список ближайших занятий (`GET /sessions/my`), диапазон 7/14/30 дней; для представителя — переключатель подопечных (в запрос уходит `studentId=<ward>`); `/sessions/my` → редирект на `/my/schedule` |
| **Шаблоны расписания**     | `/study-groups/:id/schedule` | ✅      | CRUD шаблонов, предпросмотр генерации (ToCreate/Skipped/конфликты), применение — готово |
| **Посещаемость**           | `/attendance`                | ✅      | сетка ученики × занятие, массовая отметка одним запросом, дефолт «Был» — готово |
| **Аудитории**              | `/school/rooms`             | ✅      | CRUD, флаг «онлайн» (вне проверки конфликтов) — готово                |
| **Нерабочие дни**          | `/school/non-working-days`  | ✅      | список/добавление/удаление, гейт правом шаблонов — готово             |
| **Счета учеников**         | `/payments/invoices`         | ✅      | список, фильтры (ученик/группа/статус/период/долг/поиск), пагинация+сортировка, мастер массового выставления, массовое выставление отмеченных — готово |
| **Карточка счёта**         | `/payments/invoices/:id`     | ✅      | построчный редактор черновика (ReplaceLines), жизненный цикл (выставить/отменить/PDF), блок оплат: подтверждение (с чеком), сторнирование — готово |
| **Тарифы**                 | `/payments/tariffs`          | ✅      | список + фильтр активности, создание/правка (kind/currency только в создании, поля пакета только для PerPackage), деактивация — готово |
| **Должники**               | `/payments/debtors`          | ✅      | отчёт `/reports/debtors`, фильтр по группе, ссылки на карточки учеников — готово |
| **Поступления**            | `/payments/revenue`          | ✅      | отчёт `/reports/revenue` за период, разбивка по способу оплаты — готово |
| **Мои счета**              | `/my/invoices`               | ✅      | список со статус-фильтром (`GET /student-invoices/my`); для представителя — столбец «Ученик» и клиентский фильтр по выбранному подопечному; `/student-invoices/my` → редирект на `/my/invoices` |
| **Настройки школы**        | `/school/settings`           | ✅      | отдельный раздел сайдбара «Школа»; часовой пояс + валюта через `GET/PUT /api/v1/tenants/settings` (`SchoolSettings.View`/`.Manage`), read-only без `.Manage`; блок «Доступ к материалам» — тумблер `restrictMaterialsOnDebt` + грейс `debtGraceDays` (EDX-015); блок «Нумерация счетов» — поле `invoiceNumberTemplate` (плейсхолдеры `{YYYY}`/`{YY}`/`{MM}`/`{N…}`) с клиентской валидацией и предпросмотром следующего номера, политика обнуления выводится из шаблона (EDX-014, разблокировано [[EDX-013 Настраиваемый шаблон номера счёта]]); нерабочие дни и аудитории — соседние пункты. Старый `/settings/school` → редирект |

## clients/admin

> Приложение полностью локализовано на русский (весь UI, не только тенант-текст) и
> перебрендировано под Edvantix (`brand-mark`, страницы входа). Терминология «Tenant» →
> «Школа» во всём видимом тексте.

| Экран | Статус | Что сделать |
|---|---|---|
| Школы | ✅ | «Школа» во всём UI; на карточке — блок показателей (активные ученики/преподаватели/группы/занятия за месяц/объём файлов) из снимков `GET /api/v1/billing/usage?tenantId=` (аккуратно «данных пока нет», если снимков ещё нет) |
| Планы | ✅ | лимиты и ставки за превышение в терминах учеников/преподавателей/групп/занятий (`RESOURCE_LABEL`); поле «Описание» плана в форме и на карточке |
| Счета платформы | ✅ | локализация; русские метки статусов/типов (`src/lib/billing-labels.ts`) |
| Подписки | ✅ | локализация |
| Пользователи, роли | ✅ | локализация; каталог прав `PERMISSION_CATALOG` на русском |
| Аудит | ✅ | локализация; русские метки типов/важности (`src/lib/audit-labels.ts`) |
| Вебхуки | ✅ | диалог создания подписки: чек-лист событий из `GET /api/v1/webhooks/event-types`, группировка по модулям (`src/lib/webhook-labels.ts`), свободный ввод для forward-compat, тумблер `*` |
| Имперсонация | ✅ | локализация |
| Дашборд | ✅ | «Обзор платформы»: KPI школ (всего/активные/истекают ≤45 дн/у лимитов ≥80%), школы по тарифам (fan-out `GET /tenants/{id}/status`), истекающие подписки, приближающиеся к лимитам (снимки `GET /billing/usage`); рост — нет `createdUtc` у школ, поле опущено |
| Демо-аккаунты | ✅ | операторские аккаунты корневой школы (`admin@root.com` + `superadmin@root.com`), персоны под Edvantix, синхронно с `DemoSeeder.BuildRootUsers()`. Роли школы (менеджер/преподаватель/представитель/ученик) — в dashboard, не в операторской консоли (изоляция тенантов) |
| Брендирование | ✅ | `components/brand-mark.tsx` + лого на страницах входа — Edvantix |

## Связанное

[[Dashboard (школа)]] · [[Admin (операторская)]] · [[Бэклог]] · [[Этапы внедрения]]
