---
key: EDX-011
aliases: [EDX-011]
tags: [задача, frontend, dashboard]
status: open
area: dashboard
priority: p3
blocked-by: []
blocks: []
related: []
created: 2026-09-03
closed:
---

# EDX-011 · Раздел/фильтр каналов учебных групп на `/chat`

## Контекст

С карточки группы есть ссылка «Чат группы» по `chatChannelId` (этап 7), но в самом
`/chat` каналы групп не выделены — их не отличить от обычных. Карта экранов: `/chat` 🔧.

## Что сделать

- [ ] В `channel-rail` — секция «Учебные группы», либо фильтр-таб. Бэкенд готов
      (EDX-010): `ChannelDto.sourceStudyGroupId` в ответе, либо запрос
      `GET /api/v1/chat/channels/my?kind=study-group` / `?kind=standalone`.
- [ ] Ссылка из строки канала на `/study-groups/:id` (по `sourceStudyGroupId`).
- [ ] Playwright на группировку.
- [ ] Обновить [[Карта экранов]] (`/chat` → ✅).

## Зависимости

- Блокируется: — (было [[EDX-010 Тип канала study-group в Chat]], готова)

## Проверка

- Aspire: создать группу → её канал появляется в разделе «Учебные группы».
