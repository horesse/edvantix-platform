---
tags: [задачи, backend, каркас]
---

# Задачи · Доработки каркаса

← [[Бэклог]]

Работы по существующим модулям. Что каждый модуль представляет собой и какие у него
контракты — в его справочнике в разделе «Модули».

> [!warning] Правило 4 из `AGENTS.md`
> `src/BuildingBlocks` не трогаем без явного согласования — общий для всех модулей,
> широкий радиус поражения. Если задача упирается в BuildingBlocks, сначала
> обсуждение, потом код.

---

## Multitenancy

Справочник: [[Multitenancy]]

> [!success] `TenantSettings` реализовано
> Сущность `Modules.Multitenancy/Domain/TenantSettings.cs` (по образцу `TenantTheme`,
> живёт в модуле, а не в защищённом `AppTenantInfo`) — `TimeZoneId` (IANA), `Currency`
> (ISO 4217), `ITenantSettingsService` на `HybridCache`, миграция с бэкофиллом
> существующих тенантов, значения по умолчанию (`UTC`/`USD`) при провижининге новой школы,
> права `SchoolSettings.Manage`/`.View`, тест изоляции по тенанту. Это разблокировало
> [[Scheduling]] и [[Payments]]. **Не делается сознательно:** колонка `Settings` (JSONB) —
> пока настроек две, типизированные колонки честнее; завести, когда появится
> третья-четвёртая (рабочие часы, формат номера счёта, поведение при задолженности).

- [x] Шаги провижининга под новые модули: базовые направления [[Curriculum]] по
      умолчанию, тариф по умолчанию [[Payments]] — реализовано. `CurriculumDbInitializer`/
      `PaymentsDbInitializer` пересмотрены с сознательных no-op'ов (комментарии "no per-tenant
      reference data to seed") на минимальный дефолт, тем же механизмом, что уже сидирует
      школьные роли (`TenantService.SeedTenantAsync` резолвит все `IDbInitializer` в scope
      тенанта). Curriculum: два верхнеуровневых `Subject` («Английский язык», «Математика») —
      нужны, потому что `CreateCourseCommand` требует `SubjectId`. Payments: один `Tariff`
      (`Kind = OneTime`, `CourseId: null`, `Amount = 0m`, `Currency` из
      `ITenantSettingsService.GetCurrentAsync()`, не хардкод `"USD"`) — `OneTime` выбран, потому
      что не зависит ни от расписания/посещаемости (`PerLesson`/`PerMonth`), ни от доп. полей
      пакета (`PerPackage`, явно исключён из дефолта). Оба идемпотентны (по `Slug`/`Name`
      соответственно, по образцу `IdentityDbInitializer`). `CourseId: null` держит модули
      независимыми друг от друга — порядок вызова двух `IDbInitializer` не гарантирован. Тесты:
      `Curriculum.Tests`/`Payments.Tests` (юнит, идемпотентность на InMemory-провайдере) +
      `Integration.Tests` →
      `TenantSeedDataTests.NewTenant_Should_BeSeededWith_DefaultSubjects_And_DefaultTariff`
      (реальный Postgres через Testcontainers) — все зелёные, `Architecture.Tests` 51/51,
      `dotnet build` 0 предупреждений/0 ошибок. Обоснование решения — `docs/02 Модули/
      Curriculum.md` и `Payments.md` (примечания в начале файлов). **Открытый шаг:** публичный
      docs-репозиторий (`github.com/fullstackhero/docs`) и changelog не обновлены в этой сессии —
      по опыту предыдущих пяти модулей это отдельная сессия с доступом к тому репозиторию.
- [ ] В UI «Tenant» → «Школа» ([[Задачи · Frontend]])

> [!note] Если всё же понадобится поле в `AppTenantInfo`
> Прецеденты есть — `Plan` и `QuotaLimits` там уже лежат, то есть прикладные поля
> в этот класс добавляли. Но это отдельное решение с явным согласованием, полным
> прогоном тестов и правкой всех модулей. По умолчанию — `TenantSettings`.

---

## Identity

Справочник: [[Identity]]

> [!success] Сид школьных ролей реализован
> `SchoolRoleConstants` (`SchoolAdmin`/`Manager`/`Teacher`/`Student`/`Guardian`) в
> `Modules.Identity.Contracts`, сборка прав фильтром по `Action`/`Resource` над
> `PermissionConstants.All` (не явным списком — новые модули подхватываются
> автоматически), сидирование в `IdentityDbInitializer` + донасыщение при каждом
> старте через `RolePermissionSyncer` (актуально: роли уже получили полный набор прав
> People/Curriculum/StudyGroups/Scheduling/Payments по мере релиза этих модулей).
> `SchoolAdmin` = бандл `Admin`. `Users.Invite` — добавлено, в бандле `Manager`.
> Гвард «последнего админа» остаётся завязан только на `RoleConstants.Admin` —
> отдельного гварда на `SchoolAdmin` нет и не планируется (обычная нессистемная роль).

### Спецификация «Приглашение по e-mail»

> [!note] Мягкая зависимость от [[People]] снята
> Изначально план был не блокироваться на People (модуля ещё не было). People теперь
> реализован — привязку приглашённого пользователя к `Guardian`/`Student` можно делать
> сразу через существующий `LinkStudentUserCommand`-подобный вызов, отдельного шага
> ждать не нужно.

| Что                         | Как                                                                                                                                                                                                                | Образец в коде                                                                                                                                  |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| Не новая сущность токена    | Переиспользовать `UserManager.GeneratePasswordResetTokenAsync` — уже с TTL (`DataProtectionTokenProviderOptions`) и одноразовый по факту (инвалидируется сменой security stamp)                                    | механизм уже используется в `ForgotPasswordCommand`/`ResetPasswordCommand`                                                                      |
| Новая команда               | `InviteUserCommand` (email, ФИО, роль) — создаёт `FshUser` со случайным неиспользуемым паролем и `EmailConfirmed = false`, затем сразу генерирует reset-токен и письмо                                             | `RegisterUserCommand` как основа состава полей, но без `Password`/`ConfirmPassword`                                                             |
| Приём приглашения           | Существующий `ResetPasswordCommand` (email + токен + новый пароль). Дополнительно — проставлять `EmailConfirmed = true` при успешном приёме приглашения (получение письма на закрытый ящик уже подтверждает адрес) | `ResetPasswordCommand` handler                                                                                                                  |
| Право на отправку           | Новое `IdentityPermissions.Users.Invite`, отдельное от `Users.Create`                                                                                                                                              | по таблице [[Модель прав доступа]] `Manager` не имеет `Users.*`, кроме этого исключения — приглашать нужно, полный доступ к пользователям — нет |
| Письмо                      | Шаблон через `BuildingBlocks/Mailing` — та же проверка механизма подстановки, что уже стоит в бэклоге [[Notifications]] («Шаблоны сообщений с подстановкой»); не дублировать исследование                          | `ForgotPasswordCommand` уже шлёт письмо тем же каналом                                                                                          |
| Самостоятельная регистрация | `/self-register` не удаляем и не трогаем backend — просто не вызываем из dashboard UI. Инвайт становится единственным путём получить доступ представителю/ученику на практике                                      | `SelfRegisterUserEndpoint` (анонимный, уже рейт-лимитирован)                                                                                    |

- [x] `InviteUserCommand` + валидатор + `InviteUserEndpoint`
      (`RequirePermission(IdentityPermissions.Users.Invite)`)
- [x] Письмо-приглашение через `BuildingBlocks/Mailing`, ссылка вида
      `{Origin}/accept-invite?email=…&token=…` (плюс `tenant=…`, не значился в
      спецификации явно, но нужен: `/reset-password` требует `X-FSH-App`/tenant, как и
      уже существующая `/reset-password` ссылка из `ForgotPasswordCommand`)
- [x] Проставление `EmailConfirmed = true` при успешном `ResetPasswordCommand`
      по приглашению — без отдельного флага «это был приём приглашения»: у обоих
      сценариев один и тот же токен (`GeneratePasswordResetTokenAsync`), различить их
      нечем и незачем — `EmailConfirmed == false` уже однозначно значит «ждёт приёма
      инвайта». Подробности и обоснование — [[Identity]] → «Приглашение по e-mail»
- [x] Привязка приглашённого пользователя к `Guardian`/`Student` — People уже
      реализован, используются существующие `LinkStudentUserCommand`/`LinkGuardianUserCommand`
      (`.../link-user`, вызов на стороне клиента: Identity не может ссылаться на People)
- [ ] dashboard: страница `/accept-invite` — форма установки пароля по ссылке.
      Перенесено в [[Задачи · Frontend]] → Этап 6 «Кабинеты» — backend полностью готов
- [ ] dashboard: убрать/не строить экран самостоятельной регистрации.
      Перенесено в [[Задачи · Frontend]] → Этап 6 «Кабинеты»
- [x] Тест: письмо не отправляется на уже существующий email (юнит,
      `UserRegistrationServiceTests` — дубликат email роняет `CreateAsync` раньше, чем
      сгенерирован токен/поставлено письмо в очередь); токен одноразовый, повторное
      использование — 400 (интеграционный тест на реальном Postgres,
      `InviteUserFlowTests` — одноразовость даёт инвалидация security stamp внутри
      `UserManager.ResetPasswordAsync`, юнит-тест с моком `UserManager` этого не поймает)

**Не делалось в первой итерации (решение подтверждено реализацией):** поле «роль при
приглашении» ограничено набором сидированных школьных ролей (`SchoolRoleConstants.All`),
произвольную роль ввести нельзя — гарантируется валидатором команды.

> [!note] Публичный docs-репозиторий и changelog — открытый пункт
> Правило 10 из `AGENTS.md`: `github.com/fullstackhero/docs` и changelog для этой фичи
> не обновлялись в этой сессии — по опыту предыдущих пяти модулей (People, Curriculum,
> StudyGroups, Scheduling, Payments) это отдельная сессия с доступом к тому репозиторию.

### UI «Groups» → «Группы доступа»

Чисто фронтенд, без изменений в API. Уже отслеживается пунктом
«`/identity/groups` — переименовать в «Группы доступа»» в [[Задачи · Frontend]] —
здесь не дублируется, только напоминание не путать со [[StudyGroups]].

---

Ядро аутентификации, JWT, политику паролей, сессии — не трогаем.

---

## Chat

Справочник: [[Chat]]

- [ ] Обработчик `StudyGroupCreatedIntegrationEvent` → приватный канал,
      `ChatChannelId` в группу
- [ ] Обработчики `StudentEnrolled` / `StudentUnenrolled` → синхронизация участников
- [ ] **Учёт учеников без учётной записи**: `ChannelMember.UserId` требует `FshUser`;
      при `Student.UserId == null` добавлять представителя-плательщика, а не падать
- [ ] Архивация канала при `StudyGroupFinished` — чтение остаётся, запись запрещена
- [ ] Ограничение личных сообщений: ученик ↔ свои преподаватели и менеджеры;
      представитель ↔ преподаватели подопечных и менеджеры; между учениками —
      настройка школы, по умолчанию выключено

---

## Notifications

Справочник: [[Notifications]]

- [x] Шаблоны сообщений с подстановкой. В `BuildingBlocks/Mailing` шаблонизатора нет
      (подтверждено), трогать его без согласования нельзя (правило 4) — механизм сделан
      внутри модуля: `Modules.Notifications/Templating/` (`NotificationTypes` —
      стабильные ключи типов = ключи шаблонов; `NotificationTemplateCatalog` — по одному
      `NotificationTemplate` на тип из «Каталога уведомлений»; `INotificationTemplateRenderer`
      — подстановка `{{token}}`, HTML-экранирование только для тела письма). Намеренно
      минимальный: без условий/циклов. `MentionedInChannelIntegrationEventHandler` переведён
      на рендерер. Тесты — `src/Tests/Notifications.Tests/` (12/12), `Architecture.Tests`
      51/51, `dotnet build` 0/0. Справочник — [[Notifications]] → «Шаблоны сообщений с
      подстановкой», `.agents/rules/modules/notifications.md`. Публичный docs-репозиторий и
      changelog — отдельная сессия (правило 10).
- [ ] Обработчики событий [[Scheduling]], [[Payments]], [[StudyGroups]] по каталогу
      уведомлений из справочника. **Готова инфраструктура** (шаблоны + `Templating/`,
      каналы + `INotificationDispatcher` — два пункта ниже), сами подписчики ещё нет.
      Формулировка «недостаёт только подписчиков» оказалась заниженной — при разборе
      всплыло:
      1. **Резолв адресатов.** Событие несёт `StudentId`/`PayerGuardianId`, а уведомить
         надо ученика + опекунов (+ преподавателя для отмены/переноса). Нужен новый
         batch-контракт в [[People]] (по образцу `IPeopleLookupService`):
         `GetStudentNotificationRecipientsAsync(studentIds)` → на ученика: его `UserId`
         (может быть `null`) + `Email`, плюс опекуны с `UserId`/`Email`/пометкой
         «плательщик»; и `GetTeacherRecipientAsync(teacherId)`. E-mail берётся из
         People (Student/Guardian хранят `Email`), in-app требует `UserId`.
      2. **Полей в событиях не хватает под тексты каталога.** `SessionCancelledIntegrationEvent`
         не несёт времени занятия (`{{date}}`/`{{time}}`), Payments-события — номера счёта,
         валюты, имени ученика. Либо аддитивно обогатить события у издателей (Scheduling/
         Payments + их тесты + справочники), либо упростить копирайт. Решение — за
         владельцем задачи.
      Ключи типов и шаблоны уже заведены под все 12 строк каталога
      (`NotificationTypes` / `NotificationTemplateCatalog`).
- [ ] Настройки подписок пользователя — страница есть, наполнения нет. Нужна сущность
      `NotificationPreference` (тип × канал, вкл/выкл), миграция, эндпоинты
      GET/PUT, учёт в `INotificationDispatcher` (по умолчанию включены только отмена/
      перенос занятия, счёт, задолженность — см. предупреждение в [[Notifications]]).
- [ ] Дедупликация и группировка: 10 отменённых занятий → одно сводное письмо,
      окно агрегации 5–10 минут. Нужна буферная сущность + Hangfire-задание на
      флаш окна; строится поверх подписчиков.
- [x] Абстракция `INotificationChannel` — `Modules.Notifications/Channels/`.
      `NotificationChannelKind` (`[Flags]`: `InApp`/`Email`/…), `INotificationChannel`
      (`Kind` + `SendAsync`), реализации `InAppNotificationChannel` (строка `Notification`
      + SignalR, не best-effort) и `EmailNotificationChannel` (`BuildingBlocks/Mailing`,
      best-effort, только если у шаблона есть тело письма). `INotificationDispatcher`
      рендерит шаблон один раз и разливает по запрошенным каналам, сверяя ambient-тенант.
      `MentionedInChannelIntegrationEventHandler` переведён на диспетчер. Telegram/SMS
      позже — одна регистрация `INotificationChannel` + бит в enum. Тесты —
      `src/Tests/Notifications.Tests/Channels/` (21/21 суммарно по проекту),
      интеграционные mention/notifications 22/22, `dotnet build` 0/0. Справочник —
      [[Notifications]] → «Каналы доставки», `.agents/rules/modules/notifications.md`.
- [ ] Тихие часы по времени школы

---

## Webhooks

Справочник: [[Webhooks]]

- [ ] Пополнить каталог типов событий: `Student*`, `StudyGroup*`,
      `Student(Enrolled|Unenrolled)`, `Session*`, `Attendance*`,
      `StudentInvoice*`, `StudentPayment*` — [[Интеграционные события]]
- [ ] **Публичные схемы тел** `WebhookPayloads/v1/`. Внутренние DTO переиспользовать
      нельзя: меняются при рефакторинге, а интегратор к телу привязан
- [ ] Проверить, что тенантные подписки школы работают: сейчас право
      `Platform.Webhooks` — root-уровня, а школе нужны свои
- [ ] Документация полей для интеграторов — в публичный docs-репозиторий

`TestWebhookSubscriptionCommand` уже есть — тестовая отправка реализована.

---

## Files

Справочник: [[Files]]

> [!success] `LessonMaterialAccessPolicy` реализована
> Сделана вместе с [[Curriculum]] (`Authorization/LessonMaterialAccessPolicy.cs`) по
> образцу `ProductFileAccessPolicy`. Политика доступа к чекам об оплате тоже сделана —
> `Authorization/PaymentProofAccessPolicy.cs` в [[Payments]].

- [ ] Лимиты объёма по плану через `BuildingBlocks/Quota`
- [ ] Ограничение типов файлов для материалов уроков
- [ ] Ограничить прямую загрузку видео по размеру: гигабайты на MinIO без
      транскодирования и адаптивного стриминга — дорого и неудобно.
      Для видео `MaterialKind = Video` со ссылкой на внешний хостинг (домен решён
      в [[Открытые вопросы]], реализация в `LessonMaterial` ещё не сделана)

---

## Billing

Справочник: [[Billing]]

- [ ] Метрики `UsageSnapshot` под предметную область: активных учеников,
      преподавателей, групп, занятий за месяц, объём файлов. По ним строятся планы.
      Сущность/задание `CaptureUsageSnapshotsCommand` уже есть как общий механизм —
      предметных метрик в нём нет
- [ ] Соблюдение лимитов через `BuildingBlocks/Quota`: при превышении — мягкая
      блокировка создания новых сущностей с явным сообщением, без потери доступа
      к существующим данным
- [ ] Названия планов и тексты под школьную тематику

---

## Tickets

Справочник: [[Tickets]]

- [ ] Категории обращений: «Оплата», «Расписание», «Смена группы»,
      «Качество преподавания», «Техническая проблема». Категория определяет
      адресата по умолчанию
- [x] Привязка к контексту: `RelatedStudentId`, `RelatedStudyGroupId`,
      `RelatedInvoiceId` — nullable-поля на `Ticket` (непрозрачные id, без рантайм-ссылок
      на другие модули), проброшены в `CreateTicketCommand`/`UpdateTicketCommand`/`TicketDto`
      и в фильтры `SearchTicketsQuery` (+ `GET /tickets`). `PUT` — полная замена; `Guid.Empty`
      нормализуется в null и отклоняется валидатором. Частичные индексы `... IS NOT NULL`.
      Миграция `TicketRelatedContext`. Тесты — `Integration.Tests/Tests/Tickets/
      TicketRelatedContextTests.cs` (4), Tickets-интеграция 52/52, `Architecture.Tests` 51/51,
      `dotnet build` 0/0. Справочник — [[Tickets]], `.agents/rules/modules/tickets.md`.
- [ ] Разделение адресата: обращения к школе против обращений к платформе —
      поле `Audience` или отдельные очереди
- [ ] Вложения: в контрактах команд для них ничего нет. Проверить и добавить
      через [[Files]]

SLA и эскалацию не делаем — для школы на 200 учеников избыточно.

---

## Auditing

Справочник: [[Auditing]]

- [ ] Ручные записи через `IAuditClient` для не-CRUD действий: выгрузка списка
      должников, просмотр чувствительных данных — сущности не меняются,
      а в журнале быть должно
- [ ] Читаемые названия сущностей и полей в UI: сейчас показываются имена классов,
      нужен словарь локализации («Ученик», «Счёт», «Занятие»)
- [ ] Просмотр истории конкретной сущности — «история этого ученика» на карточке:
      фильтр по `EntityType` + `EntityId`
- [ ] Настроить `AuditRetentionOptions` под ожидаемый объём: 200 учеников ×
      занятия × посещаемость растут быстро. Механизм уже есть, нужна политика
      и её отражение в плане [[Billing]]

## Связанное

[[Бэклог]] · [[Этапы внедрения]] · [[Задачи · Новые модули]]
