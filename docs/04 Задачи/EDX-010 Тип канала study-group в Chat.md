---
key: EDX-010
aliases: [EDX-010]
tags: [задача, backend]
status: open
area: backend
priority: p3
blocked-by: []
blocks: []
related: []
created: 2026-09-03
closed:
---

# EDX-010 · Признак «канал учебной группы» в Chat

## Контекст

Chat создаёт приватный канал на `StudyGroupCreated` и синхронизирует участников на
enroll/unenroll, блокирует канал на `StudyGroupFinished` (см. `.agents/rules/modules/chat.md`).
Но у канала нет типа/признака «study-group» — во frontend невозможно отфильтровать
каналы групп от обычных. Карта экранов помечает `/chat` как 🔧 по этой причине.

## Что сделать

- [ ] Добавить каналу необязательную привязку к контексту: `SourceKind` (`StudyGroup`) +
      `SourceId` (или узкое `StudyGroupId`), проставляется подписчиком `StudyGroupCreated`.
      Аддитивно, существующие каналы не трогаем.
- [ ] Фильтр в `ListChannelsQuery` (`kind=study-group` / `sourceKind`).
- [ ] Миграция + бэкофилл для уже созданных групповых каналов (по имени/метаданным).
- [ ] Тест изоляции тенантов, обновить `docs/02 Модули/Chat.md`.

## Зависимости

- Блокируется: —
- Блокирует: [[EDX-011 Раздел каналов учебных групп в чате]]

## Проверка

- Интеграционный тест: создать группу → канал помечен, попадает в отфильтрованный список.
